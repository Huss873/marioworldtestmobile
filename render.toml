[build]
command = """
apt-get update
apt-get install -y build-essential python3-dev python3-pip portaudio19-dev
pip install --upgrade pip setuptools
pip install -r requirements.txt
"""

[start]
command = "python app.py"
